# AIcapture 后端依赖
# 在虚拟环境中安装：
# pip install -r requirements.txt

fastapi>=0.95.0
uvicorn[standard]>=0.22.0
openai>=1.0.0
pillow>=9.5.0
python-multipart>=0.0.6
aiofiles>=23.1.0
volcenginesdkarkruntime>=0.1.0

# 说明：如果无法通过上面的包名安装火山引擎 SDK，请改用以下安装方式：
# pip install "volcengine-python-sdk[ark]"
